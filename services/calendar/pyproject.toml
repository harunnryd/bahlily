[project]
name = "bahlily-calendar"
version = "0.1.0"
description = "Calendar sync and local meeting auto-start"
authors = [
    { name = "Harun", email = "harun.sigmawaveai@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
bahlily-calendar = "bahlily_calendar:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
